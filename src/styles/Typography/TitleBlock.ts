import styled from '@emotion/styled';

export const TitleBlock = styled.h2`
  font-family: var(--font-secondary), Roboto, sans-serif;
  font-size: 40px;
  font-weight: 500;
  letter-spacing: 0;
  text-transform: uppercase;
  color: #312420;
  border-bottom: 2px solid #3124205e;
`;
